import { StyleSheet, } from 'react-native';

const styles = StyleSheet.create({
    safeAreaViewTop: {
        flex: 0,
        padding: 0,
        backgroundColor: '#B43235',
    },
    safeAreaViewBottom: {
        flex: 1,
    }
});

export default styles